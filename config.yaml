telegram:
  bot_token: "YOUR_BOT_TOKEN_HERE"

settings:
  fetch_depth: 0 # 0 means unlimited
  download_workers: 3
  yt_concurrent_fragments: 2
  retry_uploads: 1
  delay_between_creators_seconds_min: 10
  delay_between_creators_seconds_max: 30
